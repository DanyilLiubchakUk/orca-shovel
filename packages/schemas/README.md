# packages/schemas

Shared schema package.

Responsibilities:

- validation schemas
- DTOs crossing app/server/task boundaries
- AI structured output schemas
- tool input/output schemas
- enum definitions

Rule: schemas should be dependency-light and safe for server/shared imports.

