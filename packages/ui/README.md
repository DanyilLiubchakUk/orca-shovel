# packages/ui

Shared UI package.

Responsibilities:

- shadcn/ui components
- design primitives
- dashboard layout primitives
- reusable memory/source/reminder UI components

Rule: this package must not import server-only code or secrets.

